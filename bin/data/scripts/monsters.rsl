class MonsterAI

	func GoblinGuard(monster)
		if monster.CanSeePlayer() then
			if monster.CanAttackPlayer() then
				monster.AttackPlayer()
			elseif (monster.CanMoveTowardPlayer())
				monster.MoveTowardPlayer()
			end
		else
			monster.Wander()
		end
	end
	
end